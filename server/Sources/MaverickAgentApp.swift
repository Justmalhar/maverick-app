// server/Sources/MaverickAgentApp.swift
//
// Placeholder entry point so the MaverickAgent application target links.
// Future tasks will replace this with the real menu-bar / agent app.
import SwiftUI

@main
struct MaverickAgentApp: App {
    var body: some Scene {
        Settings {
            EmptyView()
        }
    }
}
